package com.nidoapp.nidopub.job




/**
 * Job que comprueba los eventos de salas que comienzan en 3 horas, y les
 * envía un email con el listado de ventas en un excel.
 * @author Developer
 *
 */
class TicketsSoldEMailSendJob {
	/*static triggers = {
		// Cada 30 minutos 	0 0/30 * * * ?
		// Cada minuto 		0 0/1 * * * ?
		cron name: 'TicketsSoldEMailSendJob', cronExpression: "0 0/1 * * * ?"
	}

	def ticketsSoldEMailSendService

    def execute() {
		log.info "Sistema automático de control ventas de eventos de salas que se ha cerrado la venta de entradas..."
		
        ticketsSoldEMailSendService.sendTicketsSoldEMail()
		
    }*/
}
